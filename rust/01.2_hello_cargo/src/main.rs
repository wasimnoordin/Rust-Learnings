fn main() {
    println!("Hello, world!");
}

/*

cargo build -> create exe in target/debug/hello_cargo
    --release: to compile with optimisations
cargo run -> run cargo project -> OR: ./target/debug/hello_cargo
cargo check -> check if code compiles

*/ 